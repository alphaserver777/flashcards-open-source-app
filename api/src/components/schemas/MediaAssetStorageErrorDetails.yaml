type: object
additionalProperties: false
required:
  - workspaceId
  - mediaAssetId
  - reason
  - retryable
properties:
  workspaceId:
    $ref: ./UuidString.yaml
  mediaAssetId:
    $ref: ./UuidString.yaml
  reason:
    type: string
    enum:
      - upload_not_available
      - storage_temporarily_unavailable
  retryable:
    type: boolean
