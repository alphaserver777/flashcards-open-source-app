type: object
additionalProperties: false
required:
  - kind
  - cardIds
properties:
  kind:
    type: string
    enum:
      - explicitCardIds
  cardIds:
    type: array
    minItems: 1
    maxItems: 5000
    items:
      $ref: ./UuidString.yaml
